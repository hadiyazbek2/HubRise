<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Exclude DataStore (tokens) so auth state is never restored from backup -->
    <exclude domain="file" path="datastore" />
</full-backup-content>
